<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/style.css">
    <title>Register for Event</title>
</head>
<body>
    <header class="header">
        <div class="container">
            <h1>Register for <%= event.title %></h1>
        </div>
    </header>

    <section class="registration-container">
        <div class="registration-card">
            <h2>Event Details</h2>
            <p><strong>Title:</strong> <%= event.title %></p>
            <p><strong>Date:</strong> <%= new Date(event.date).toDateString() %></p>
            <p><strong>Category:</strong> <%= event.category %></p>
            <p><strong>Description:</strong> <%= event.description %></p>
            <p><strong>Attendees:</strong> <%= event.attendees %> / <%= event.maxAttendees %></p>

            <form action="/register/<%= event.id %>" method="POST" class="register-form">
                <label for="userName">Your Name:</label>
                <input type="text" id="userName" name="userName" required>
                
                <label for="email">Your Email:</label>
                <input type="email" id="email" name="email" required>
                
                <button type="submit" class="button">Register Now</button>
            </form>
        </div>
    </section>

    <footer class="footer">
        <p>&copy; <%= new Date().getFullYear() %> Event Registration System. All rights reserved.</p>
    </footer>
</body>
</html>
