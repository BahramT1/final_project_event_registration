<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/style.css">
    <title>Registration Confirmation</title>
</head>
<body>
    <div class="confirmation-container">
        <div class="confirmation-card">
            <h1>Registration Successful!</h1>
            <p>Thank you for registering for the event. We have received your details and you are now added to the attendee list.</p>

            <div class="confirmation-details">
                <h2>Your Details:</h2>
                <p><strong>Name:</strong> <%= userName %></p>
                <p><strong>Email:</strong> <%= email %></p>
                <p><strong>Event:</strong> <%= eventTitle %></p>
                <p><strong>Date:</strong> <%= new Date(eventDate).toDateString() %></p>
            </div>

            <div class="confirmation-actions">
                <a href="/" class="button">Back to Home</a>
                <a href="/register/<%= eventId %>" class="button">Register Another Person</a>
            </div>
        </div>
    </div>
</body>
</html>
